# PRD: 个人自媒体创作台 — Canvas Runtime（无限画布 + 节点创作 Pipeline）

> 来源文档：
> - [`creator-workspace-prd-runtime-spec-v0.2.md`](../../creator-studio/docs/canvas-runtime/creator-workspace-prd-runtime-spec-v0.2.md)
> - [`creator-workspace-ui-interaction-spec-v0.2.md`](../../creator-studio/docs/canvas-runtime/creator-workspace-ui-interaction-spec-v0.2.md)
> - [`node-runtime-architecture-v0.1.md`](../../creator-studio/docs/canvas-runtime/node-runtime-architecture-v0.1.md)
> 生成日期：2026-08-10
> 状态：待评审

## 1. Introduction / Overview

创作者从「灵感 → 选题 → 调研 → 大纲 → 脚本 → 图片 → 配音 → 视频 → 发布」的完整过程，目前散落在多个工具里：Obsidian 存灵感、飞书写草稿、各平台后台做发布，AI 工具各自为战。本 feature 构建一个 **AI 原生创作工作台**，用一个「多项目工作区 + 无限画布」把所有内容生产环节组织进同一空间。

画布上的每个 **Node** 代表一个内容产物（选题、大纲、脚本、封面、配音、视频……），而不是一个操作按钮。AI 能力通过 **Operation** 挂载：在右侧 Inspector 选中一个节点，就能对它执行「生成大纲 / 润色 / 生成封面 / 生成配音」等操作，执行产生新的内容节点或版本。这样创作者不必切工具，创作过程天然留痕、可回溯、可分支。

核心价值：**把「内容是什么」和「怎么加工内容」解耦**，让创作管线可以被 AI 逐步接管，同时所有中间产物都在画布上可见、可继续。

本 PRD 只定义「要什么」；「怎么实现」见 `.spec/` 下的多个技术规格。

## 2. Goals

- 建立一个可运行、可扩展的 **Project + Infinite Canvas + Node + Inspector + Operation Runtime** 骨架。
- 跑通至少三条内容链路：`Topic → Outline → Script`、`Script → Cover/Image`、`Script → Voice → Video`。
- AI Operation 通过 **Registry** 注册，新增能力不修改 Node 组件（可插拔）。
- 同一内容的修改走 **Version**，不同方向走 **Branch**，多候选走 **Collection**，画布不因此节点爆炸。
- 画布只负责展示结果、关系、状态，不承担重编辑器；复杂编辑收敛到 Inspector / 高级编辑器。
- 从第一版就考虑大画布性能（视口剔除、LOD、媒体懒加载）与异步生成、并发、失败恢复。

## 3. User Stories

### US-001: 创建内容项目
**Description:** As a 创作者, I want to 创建一个项目并设置名称、内容类型、目标平台，以便在一个空间里组织一条内容的生产。

**Acceptance Criteria:**
- [ ] 可新建 Project，填写标题、内容类型、目标平台
- [ ] 可设置 Project Context，可选绑定 Personal Style
- [ ] 创建后进入该 Project 的 Canvas 工作区
- [ ] Project 可归档/恢复
- [ ] Typecheck/lint 通过

### US-002: 多项目标签切换
**Description:** As a 创作者, I want to 同时打开多个项目标签并在其间快速切换，以便并行推进多条内容线。

**Acceptance Criteria:**
- [ ] 顶部可同时打开多个 Project Tab，显示名称、Dirty 状态、当前 Run 状态、未完成任务数
- [ ] 切走的项目不保留完整 Canvas DOM，但保留 viewport/selection/打开的 Inspector 节点
- [ ] 后台 Run 切走继续执行，切回时恢复
- [ ] 关闭标签不丢失已持久化内容
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-003: 在画布上组织创作节点
**Description:** As a 创作者, I want to 在无限画布上创建、移动、连接、分组节点，以便按自己的思路组织内容流程。

**Acceptance Criteria:**
- [ ] 画布支持 Pan / Zoom，Node 可拖动、多选、复制、删除、分组
- [ ] 可从画布空白处（双击 / +）创建节点
- [ ] 可从当前节点拖出 Edge 连接到另一个节点
- [ ] Edge 表达输入语义（如 Topic ──topic──→ Outline），不是纯视觉线
- [ ] Canvas 空状态显示引导（创建选题/输入文字/上传素材）
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-004: 从 Inspector 执行 AI Operation
**Description:** As a 创作者, I want to 选中节点后在 Inspector 看到可执行的操作并发起生成，以便让 AI 参与创作。

**Acceptance Criteria:**
- [ ] 单击节点打开 Inspector，展示 Artifact 摘要与可执行 Operation
- [ ] Operation 由 Registry 自动计算（依据 kind/role/上下文），不在组件里写死
- [ ] CREATE 产生下游新 Artifact + Canvas Node + Edge
- [ ] TRANSFORM 产生同一 Artifact 的新 Version，不新增节点
- [ ] BRANCH 产生并行方向的新节点
- [ ] ACTION 执行副作用（如 publish）
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-005: 查看 Run 状态与结果
**Description:** As a 创作者, I want to 看到 AI 生成的排队/进行中/完成/失败状态，以便知道什么时候可以继续。

**Acceptance Criteria:**
- [ ] Run 状态机：queued → running → completed / failed / cancelled（可含 waiting_input）
- [ ] 生成中 UI 显示状态，完成后一次刷新预览（文本不按 token 高频刷新）
- [ ] 失败时显示可读错误 + [重试] / [修改参数]，禁止裸抛堆栈
- [ ] Retry 创建新 Run，不覆盖原 Run
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-006: 版本历史与恢复
**Description:** As a 创作者, I want to 查看某内容的历史版本并恢复，以便回退到满意的状态。

**Acceptance Criteria:**
- [ ] Artifact 可查看 Version History
- [ ] 恢复历史版本创建新 current 引用，不删除历史链
- [ ] Undo/Redo 覆盖画布 UI 操作（move/resize/edge/node delete/group）；AI 内容恢复走 Version
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-007: 媒体节点的轻量预览
**Description:** As a 创作者, I want to 在画布上看图片/音频/视频的轻量预览而不是完整媒体，以便画布保持流畅。

**Acceptance Criteria:**
- [ ] Image 加载低分辨率缩略图；Audio 显示波形+时长；Video 显示 poster+时长
- [ ] 点击播放才懒加载 / 挂载完整媒体与编辑器
- [ ] 大画布下（300+ 节点）基础操作保持流畅
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-008: 多候选 Collection 选择
**Description:** As a 创作者, I want to 一次生成多个候选（标题/封面/Hook 变体）并挑选，以便对比择优。

**Acceptance Criteria:**
- [ ] Collection 节点可包含多个 candidate
- [ ] 支持 Select / Compare / Regenerate / Generate More / Promote to Node / Delete Candidate
- [ ] Typecheck/lint 通过，并在浏览器验证

### US-009: Action 节点与外部副作用
**Description:** As a 创作者, I want to 用 Action 节点表达发布/导出/保存到外部系统，以便让内容生命周期可见。

**Acceptance Criteria:**
- [ ] Action 节点表达 Publish / Export / Send to Lark / Save to Obsidian 等副作用与状态
- [ ] Publish 全局入口固定在右上角 Header
- [ ] Typecheck/lint 通过，并在浏览器验证

## 4. Functional Requirements

- FR-1: 系统必须提供 Project 生命周期（创建/切换/归档/删除），每个 Project 拥有一个独立 Graph。
- FR-2: 系统必须区分 CanvasNode（表现层）、Artifact（内容层）、ArtifactVersion（历史层）、Edge（输入语义）、OperationDefinition（能力层）、Run（执行层）。
- FR-3: CanvasNode 只保存 artifactId + 布局 + UI 状态，不得保存正文全文、高清媒体、全部 Versions/Runs。
- FR-4: 系统必须用 `kind + role` 表达业务内容（如 `text/topic`、`image/cover`），不得为每种业务内容新建底层类型。
- FR-5: 所有 Operation 必须通过 Registry 注册；Inspector 根据 Registry 自动生成可执行操作列表。
- FR-6: Operation 结果行为必须统一为 CREATE / TRANSFORM / BRANCH / ACTION 四类，前端据此决定更新节点/建节点/建 Collection/展示副作用。
- FR-7: 每个 Run 必须拥有唯一 idempotency key；重复请求不得重复创建结果。
- FR-8: 系统必须提供 SSE / 事件机制推送 `run.*`、`artifact.*`、`node.*`、`edge.*` 事件，UI 通过事件更新而非轮询整个 Project。
- FR-9: 画布必须实现 Viewport Culling、LOD（按 zoom 分级渲染）、媒体懒加载与状态隔离。
- FR-10: 删除 CanvasNode 默认只移除表现实体；Artifact 无引用时标记 orphan，延迟 GC；用户明确「同时删除内容」才软删除。
- FR-11: AI 上下文必须分层组装（System + Personal Style + Project Context + Connected Inputs + References + Config + 临时指令），不允许每个 Node 自己拼 Prompt。
- FR-12: 非活动 Project 不保持完整 Canvas DOM；保存 viewport/selection/打开的 Inspector 节点。

## 5. Non-Goals

本 feature 第一阶段**不做**：

- 完整自动化 DAG Scheduler、任意循环工作流、自动执行所有下游节点
- Canvas 内完整视频 Timeline、完整富文本编辑器、专业级图片编辑
- 多人实时协同、完整评论系统、完整数据分析系统、大规模团队权限系统
- 真正的多平台发布接口（发布 API / 浏览器插件 / 油猴脚本，见 idea.md 的模块 3）

底层数据结构允许未来扩展，但 UI 不提前暴露复杂能力。

## 6. Design Considerations

视觉基准来自 `creator-workspace-ui-interaction-spec-v0.2.md`：

- **Dark / Low Saturation / Professional / Creator Tool / Canvas First / Quiet UI**
- 布局：Topbar(56~64px) + Sidebar(176~208px，可折叠为 Icon Rail) + Canvas + Inspector(320~380px，可关闭)
- 禁大面积高饱和填色；Node 颜色只用于 icon / thin border / tiny badge / active glow
- Inspector 是核心 Operation Surface，固定布局 Header/Tabs/Input Context/Preview/Config/Actions/Results/Version
- 分享 / 发布固定在右上角 Header，不作为 Canvas 悬浮主按钮
- 空状态引导「开始你的第一个创作节点」，底提示「双击画布也可以创建节点」
- 无障碍最低要求：对比可读、选中不只靠颜色、状态有 icon+text、焦点可见、icon button 有 aria-label

## 7. Technical Considerations

- 构建在 Foundation（`issues/foundation/`，FND-01~14）之上：Hono + SQLite/Drizzle + `/api/v1` REST + 错误 envelope + `revision` 乐观并发 + Task Runtime + SSE。
- 复用 `packages/contracts` 共享契约、`apps/web/src/shared/ui/` 组件库与语义 token（`--surface/--elevated/--primary/--border`）。
- 依赖 creator-profile feature 的 **Personal Style 契约**（Project 绑定 `personalStyleId`，Context Assembly 注入 Personal Style），契约在 `.feature-002-creator-profile/.spec` 定义。
- 技术细节（数据模型 / API / Runtime / Canvas 前端）见 `.feature/.feature-001-canvas-runtime/.spec/` 下的多个规格。

## 8. Success Metrics

- 跑通三条链路：Topic→Outline→Script；Script→Cover/Image；Script→Voice→Video。
- 100 Nodes 首屏可交互 < 1.5s，Pan/Zoom 视觉流畅；300 Nodes 普通操作流畅；500 Nodes 开 culling/LOD 后可用。
- 新增一个 Operation（如 `remove_background`）不需要修改 Base ImageNode。
- MVP Definition of Done（见来源文档 §59）全部满足。

## 9. Open Questions

- Personal Style 契约的具体字段与注入粒度（由 `.feature-002-creator-profile/.spec` 先行定义，本 feature 引用）。
- Provider / 模型接入方式：MVP 是否继续用 Seed Provider（沿用 Foundation Task Runtime），还是接真实 LLM/图片/TTS。
- Canvas 渲染层选型：自研 transform 画布 vs. 引入 @xyflow/react 等库（需在 `.spec/05-canvas-ui` 定案）。
- MVP 阶段 Publish 是否真实落地，还是仅 ActionNode 骨架 + Header 占位。
